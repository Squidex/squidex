apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "squidex.fullname" . }}-test-connection"
  labels:
    {{- include "squidex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "squidex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
