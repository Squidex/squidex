{
    "trigger":{
       "schemas":[
            {
                "condition":"event.type == 'Published' || (event.type == 'Updated' && event.status == 'Published')"
            }
        ],
       "handleAll":false,
       "triggerType":"ContentChanged"
    },
    "action":{
       "actionType":"Kafka"
    }
 }