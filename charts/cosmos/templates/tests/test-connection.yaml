apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cosmos.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "cosmos.name" . }}
    helm.sh/chart: {{ include "cosmos.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cosmos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
