{{- if .Values.vault_secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.vault_secrets.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "cosmos.fullname" . }}
data:
{{- range $filename, $fileContents := .Values.vault_secrets.secretFiles }}
  {{ $filename }}: {{ $fileContents | b64enc | quote }}
{{- end }}
{{- end }}